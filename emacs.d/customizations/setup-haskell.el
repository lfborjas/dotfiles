;; Temporarily disable haskell stuff, for machines where it isn't present
;(require 'haskell-interactive-mode)

                                        ;(require 'haskell-process)
                                        ;(add-hook 'haskell-mode-hook 'interactive-haskell-mode)

